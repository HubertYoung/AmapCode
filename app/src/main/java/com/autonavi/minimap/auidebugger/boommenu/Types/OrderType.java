package com.autonavi.minimap.auidebugger.boommenu.Types;

public enum OrderType {
    DEFAULT(0),
    REVERSE(1),
    RANDOM(2);
    
    int a;

    private OrderType(int i) {
        this.a = i;
    }
}
