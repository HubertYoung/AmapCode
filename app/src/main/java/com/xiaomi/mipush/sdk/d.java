package com.xiaomi.mipush.sdk;

public enum d {
    ASSEMBLE_PUSH_HUAWEI(1),
    ASSEMBLE_PUSH_FCM(2),
    ASSEMBLE_PUSH_COS(3);
    
    private int d;

    private d(int i) {
        this.d = i;
    }
}
